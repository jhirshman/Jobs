%h2="Listing for #{@job.title}"

%h4 Company
%p= @company
%p= @location

%h4 Job Description
%p= @job.description

- unless @job.externalLink?
	%h3=link_to "Apply", new_application_path(@job)
- else
	%h3
		Visit 
		=link_to @job.application_url, urlHelp(@job.application_url)
		to Apply

